<div id="page_content_inner">
<h3 class="heading_b uk-margin-bottom">Слайдеры</h3>
    <div class="md-card">
        <div class="md-card-content">
            <div class="uk-overflow-container uk-margin-bottom">
                <table class="uk-table uk-table-align-vertical uk-table-nowrap tablesorter tablesorter-altair" id="ts_pager_filter">
                    <thead>
                        <tr>
                            <th>Количество слайдов</th>
                            <th class="uk-text-center" >Название</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% @sliders.each do |slider| %>
                        <tr>
                            <td><%= slider.slides.size %></td>
                            <td class="uk-text-center" ><%= link_to slider.name, edit_administrators_slider_path(slider) %></td>
                            
                        </tr>
                        <% end %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>